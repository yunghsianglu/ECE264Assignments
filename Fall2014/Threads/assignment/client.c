#include <stdio.h> 
#include <stdlib.h> 
#include <errno.h> 
#include <string.h> 
#include <netdb.h> 
#include <sys/types.h> 
#include <netinet/in.h> 
#include <sys/socket.h> 
 
int main(int argc, char *argv[])
{
	//check the valid number of arguments. if check fails, printout "Usage: client <port number> <host IP address>"
    



    //make socket




    //set IP address and port 




	//repeat until hit ctrl+c




	//send numbers
	//while loop used heres
    //please note that your client must handle the case where the input is not valid.
    //if the input is not valid, the program should continue to the prompt message
    //for example, fgets() will return NULL if the input is not valid.
}

